---
type: reference
owner: Talent Acquisition
status: approved
last-reviewed: 2026-06-20
---

# Talent Acquisition metrics

Generated from `metrics.registry.json` — the single source of truth. Do not edit by hand.

Each metric is tagged **Core KPI**, **Diagnostic**, or **Operational alert**. The Formula cell also carries the group/aggregate form (where it differs) and the implementation protocol.

| Metric | Definition | Formula / group / protocol | Grain | Decision it supports | Agent may / must-not |
|---|---|---|---|---|---|
| **Open requisitions**<br><sub>Core KPI</sub> | Count of approved, active requisitions at the as-of date (open-approved only, not drafts). | `count(reqs where status = 'open' and approved = true and on_hold = false)`<br>_group:_ `openings_remaining = sum(openings_approved - openings_filled) when a req carries multiple openings`<br>_protocol:_ Point-in-time count of distinct req_ids; report open-approved as the headline, drafts separately. | point_in_time | Where to focus sourcing capacity this week. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Time to fill**<br><sub>Core KPI</sub> | Calendar days from requisition APPROVED to offer accepted (recruiter-controllable clock). | `median(offer_accepted_date - req_approved_date)`<br>_group:_ `report p50 and p90 by segment; track 'approval latency' (created-&gt;approved) and 'time to start' (req_approved-&gt;start_date) as companions`<br>_protocol:_ Report p50 AND p90 (never a lone mean); clock starts at req-approved; segment by job family/level/geo; pair with open-req aging so the unfilled tail stays visible. | per_req | Capacity planning and hiring-plan commit dates. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Time to hire**<br><sub>Core KPI</sub> | Calendar days from a candidate's first touch to offer accepted (candidate-experience clock). | `median(offer_accepted_date - first_touch_date)`<br>_protocol:_ first_touch = min(application_date, first_recruiter_contact_date); report p50 and p90 over hired candidates. | per_hire | Candidate-experience and process-speed improvements. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Requisition aging**<br><sub>Operational alert</sub> | A requisition is 'aging' when it has been open (from approved) longer than a self-calibrating threshold; the example agent uses a flat fallback. | `days_open(from approved) &gt; k * median_time_to_fill(job_family, level, geo)   (flat fallback: days_open &gt; aging_threshold_days)`<br>_protocol:_ Self-calibrating k ~= 1.5 x segment median TTF is preferred; flat aging_threshold_days is a documented fallback the ta-reporting example uses. | per_req | Which searches need intervention or re-scoping. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Requisition staleness**<br><sub>Operational alert</sub> | A requisition is 'stale' when it has had no SUBSTANTIVE human pipeline activity for longer than the stale threshold. | `days_since_substantive_update &gt; stale_threshold_days`<br>_protocol:_ Substantive update = candidate added, stage change, interview scheduled, or recruiter note; exclude automated syncs. | per_req | Recruiter follow-up / pipeline hygiene. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Thin pipeline**<br><sub>Operational alert</sub> | A high-priority (P1) requisition whose stage-weighted expected hires fall below a safety factor; the example agent uses a flat active-candidate count. | `priority = 'P1' AND expected_hires &lt; safety_factor   where expected_hires = sum_stage(count_at_stage * passthrough(stage-&gt;hire))   (flat fallback: pipeline_depth &lt; thin_threshold)`<br>_protocol:_ Stage-weighted expected-yield is preferred; flat thin_threshold on active (post-screen, not dispositioned) candidates is the ta-reporting fallback. | per_req | Where to add sourcing before a search stalls. | ✓ calculate, flag_outliers, draft_summary<br>✗ make_hiring_decision |
| **Offer acceptance rate**<br><sub>Core KPI</sub> | Share of resolved written offers that were accepted, over a closed offer cohort. | `offers_accepted / (offers_accepted + offers_declined)`<br>_protocol:_ Closed cohort: offers extended in the window, measured once each reaches a final disposition; 'extended' = written offer. | cohort | Comp competitiveness and closing effectiveness. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision, recommend_pay_change |
| **Stage pass-through rate**<br><sub>Diagnostic</sub> | Share of candidates advancing from one funnel stage to the next, over a matured stage cohort. | `advanced_to_next_stage / candidates_with_final_outcome_from_stage`<br>_group:_ `cumulative funnel yield = hires / top_of_funnel_candidates (app -&gt; offer -&gt; hire)`<br>_protocol:_ Mature stage-entry cohort; handle re-entry/skips/same-day transitions explicitly. | per_stage | Where the funnel leaks. | ✓ calculate, trend, draft_summary<br>✗ make_hiring_decision |
| **Quality of hire**<br><sub>Core KPI</sub> | Composite, indexed 0-100, of post-hire success: early performance, first-year retention, hiring-manager satisfaction, and time to productivity. | `weighted_index(early_performance, twelve_month_retention, hiring_manager_satisfaction, time_to_productivity)`<br>_protocol:_ Composite indexed 0-100: each component is normalized 0-100 then combined with the params.weights (sum=1.0); publish the weights; score only matured cohorts. | hire_cohort | Whether faster/cheaper hiring is still good hiring. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Cost per hire**<br><sub>Core KPI</sub> | Fully loaded recruiting cost per hire (ANSI/SHRM-2012 standard). | `(total_internal_recruiting_cost + total_external_recruiting_cost) / number_of_hires`<br>_protocol:_ Use the ANSI/SHRM-2012 definition verbatim; state the period and the internal-cost components included. | cohort | TA efficiency and budget tradeoffs. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Source-channel effectiveness**<br><sub>Diagnostic</sub> | Yield (and downstream quality) of each recruiting source. | `hires_from_source / candidates_from_source`<br>_group:_ `pair yield with quality_of_hire_by_source = mean(quality_of_hire \| source) and cost_per_hire_by_source; rank channels on quality-adjusted yield, not raw yield`<br>_protocol:_ Pair yield with quality_of_hire and cost per channel; state the attribution model (first-touch vs last-touch). | per_channel | Where to allocate sourcing spend. | ✓ calculate, trend, segment, draft_summary<br>✗ make_hiring_decision |
| **Recruiter capacity**<br><sub>Diagnostic</sub> | Active open-approved requisitions carried per recruiter. | `count(open_approved_reqs) / count(active_recruiters)`<br>_protocol:_ Report distribution, not just the mean; weight by role difficulty where possible. | point_in_time | TA staffing and load-balancing decisions. | ✓ calculate, segment, flag_outliers, draft_summary<br>✗ make_hiring_decision |
